<div class="container-fluid">
  <div class="row">
    <div class="col-md-6 col-md-offset-3 text-center">
      <% if params[:plan] == '3' %>
        <h1>Advanced Account</h1>
        <h4>Become a member and get access to teching videos aimed at the advanced level</h4>
        <p>Chose this option if your bankroll exceeds $/€ 20000</p>
      <% elsif params[:plan] == '2' %>
        <h1>Intermediate Account</h1>
        <h4>Become a member and get access to teching videos aimed at the intermediate level</h4>
        <p>Choose this option if your bankroll is somewhere between $/€ 5000 - 20000.</p>
      <% else %>
        <h1>Basic Account</h1>
        <h4>Become a member and get access to teching videos aimed at the beginner level</h4>
        <p>Choose this option if you have a bankroll of $/€ 5000 or less.</p>
      <% end %>
    </div>
    <div class="col-md-6 col-md-offset-3">
      <div class="well register-form">
        <h3>Sign up</h3>
        <% if params[:plan] == '1' %>
          <%= render "basic_form" %>
        <% elsif params[:plan] == '2' %>
          <%= render "intermediate_form" %>
        <% else %>
          <%= render "advanced_form" %>
        <% end %>
        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</div>



